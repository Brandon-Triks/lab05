# Отчёт по лабораторной работе №2

## Тема: Системы контроля версий на примере Git

## Выполненные задачи:

1. Создан публичный репозиторий `lab02` с лицензией MIT на GitHub
2. Сгенерирован токен доступа с правами `repo`
3. Настроена конфигурация Git (user.name, user.email)
4. Инициализирован локальный репозиторий через `git init`
5. Подключён удалённый репозиторий через `git remote add origin`
6. Созданы файлы проекта через консоль Debian:
   - `sources/print.cpp` — реализация функций печати
   - `include/print.hpp` — заголовочный файл
   - `examples/example1.cpp` — пример вывода в консоль
   - `examples/example2.cpp` — пример вывода в файл
7. Добавлен файл `.gitignore` для игнорирования временных файлов
8. Все изменения отправлены на GitHub через `git push`
